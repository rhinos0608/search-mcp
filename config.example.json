{
  "//": "Copy to config.json and fill in your values. Never commit config.json. searchBackend is intentionally NOT set here: Codex is main source when credentials are available (CODEX_ACCESS_TOKEN or ~/.codex/auth.json); all configured available providers run in parallel, dedupe URLs, then optionally semantic-rerank results. SEARCH_BACKEND only controls fallback ordering.",
  "brave": {
    "apiKey": ""
  },
  "searxng": {
    "baseUrl": ""
  },
  "exa": {
    "apiKey": ""
  },
  "tavily": {
    "//": "Tavily search API (V3.4). API key free at tavily.com. Uses TAVILY_API_KEY env var.",
    "apiKey": ""
  },
  "youtube": {
    "apiKey": ""
  },
  "stackexchange": {
    "apiKey": ""
  },
  "github": {
    "token": ""
  },
  "reddit": {
    "clientId": "",
    "clientSecret": "",
    "userAgent": ""
  },
  "crawl4ai": {
    "baseUrl": "",
    "apiToken": ""
  },
  "documentParsing": {
    "//": "In-process document & multimodal parsing. On by default; auto-discovers pdf-parse/officeparser and degrades gracefully if absent. Set DOCUMENT_PARSING_ENABLED=false (or enabled=false here) to disable. Multimodal VLM tier remains opt-in.",
    "enabled": true,
    "multimodal": false,
    "maxEnrich": 3
  },
  "embeddingSidecar": {
    "provider": "sidecar",
    "baseUrl": "",
    "apiToken": "",
    "dimensions": 768,
    "codeModel": ""
  },
  "semanticCrawl": {
    "defaultMaxBytes": 52428800,
    "maxMaxBytes": 52428800
  },
  "domainTrust": {
    "//": "Opt-in domain reputation filtering. Off by default.",
    "enabled": false,
    "trustedDomains": [],
    "blockedDomains": []
  },
  "scrubContent": false,
  "llm": {
    "//": "OpenAI-compatible LLM for contextual embeddings (optional). Base URL required; API token optional. provider = model name (e.g. gpt-4o-mini, llama3).",
    "provider": "",
    "apiToken": "",
    "baseUrl": ""
  },
  "knowledgeGraph": {
    "//": "Knowledge graph configuration. Disabled by default; set KG_ENABLED=true (or enabled=true here) to register the knowledge_graph family.",
    "//dbPath": "Resolved by resolveKgDbPath(); ~ expands to your home directory. Override with KG_DB_PATH.",
    "enabled": false,
    "dbPath": "~/.cache/search-mcp/kg/kg.sqlite",
    "projection": {
      "maxEvents": 500,
      "maxAgeMs": 86400000
    },
    "solidification": {
      "minRuns": 2,
      "minEntities": 5,
      "highConfidenceOverride": 0.85,
      "minVerbatimRatio": 0.7,
      "minSourceCount": 3
    },
    "session": {
      "maxBufferItems": 20,
      "maxIdleMs": 300000,
      "captureStdio": true
    },
    "consolidation": {
      "cadenceMs": 604800000,
      "annThreshold": 200,
      "maxFamilies": 300
    },
    "relations": {
      "maxFamilies": 100,
      "maxNodesPerFamily": 100
    }
  },
  "duckduckgo": {
    "//": "Zero-key fallback search (V3.3.1). Region / safeSearch configurable via env vars.",
    "region": "us-en",
    "safeSearch": "moderate"
  },
  "ollamaSearch": {
    "//": "Opt-in Ollama web-search backend (V3.3.1). Uses SEARCH_OLLAMA_* env vars — distinct from EMBEDDING_OLLAMA_*.",
    "baseUrl": "",
    "apiKey": ""
  },
  "deepResearch": {
    "//": "Deep Research Orchestration Engine (V4.0.0). Opt-in, disabled by default.",
    "enabled": false,
    "defaultDepth": "standard",
    "maxDepth": "deep",
    "maxToolCalls": 200,
    "maxTokens": 500000,
    "baseUrl": "",
    "//baseUrl": "OpenAI-compatible base URL for the orchestrator LLM. Do NOT include /v1 — it is appended automatically (e.g. http://localhost:11434)",
    "workerBaseUrl": "",
    "//workerBaseUrl": "Optional separate base URL for the worker LLM. Falls back to baseUrl if not set. Use this when worker and orchestrator use different endpoints.",
    "model": "",
    "//model": "Orchestrator model — mid-tier for planning, evaluation, audit, synthesis (e.g. gpt-4o, claude-sonnet-4)",
    "workerModel": "",
    "//workerModel": "Worker model — cheap for extraction, classification (e.g. gpt-4o-mini, llama-3.1-8b)",
    "apiToken": "",
    "//apiToken": "Optional API token for authenticated LLM endpoints (e.g. OpenAI API key)",
    "maxTimeMs": 300000,
    "autoSave": true,
    "treeBreadth": 4,
    "treeDepth": 2,
    "treeConcurrency": 2,
    "treeContextWordLimit": 25000,
    "agentMaxIterations": 30,
    "agentMaxSubIterations": 8,
    "agentDefaultFetchMode": "summary_focus_query"
  },
  "browser": {
    "//": "CDP Browser Control (V5.2). Opt-in, disabled by default. Requires playwright-core. Three modes:",
    "//-mode-stealth": "stealth = autonomous browser session. Set browserEngine=cloak to use the optional CloakBrowser binary instead of stock Playwright Chromium.",
    "//-mode-user": "user = connect to your existing Chrome browser via CDP. Zero stealth needed — uses your real fingerprint and sessions.",
    "//-mode-profile": "profile = launch Chrome/CloakBrowser with a persistent user data directory. Pre-authenticate once, reuse across sessions.",
    "enabled": false,
    "executablePath": "",
    "headless": true,
    "viewport": { "width": 1280, "height": 720 },
    "userAgent": "",
    "proxyServer": "",
    "cdpEndpoint": "",
    "profileDir": "",
    "maxSessionTimeMs": 300000,
    "stealthEnabled": true,
    "rebrowser": false,
    "bypassCSP": false,
    "browserEngine": "playwright",
    "//browserEngine": "Set to 'cloak' to use npm package cloakbrowser as the launch backend. Install optional deps first: npm install cloakbrowser playwright-core.",
    "cloakHumanize": false,
    "cloakHumanPreset": "default",
    "cloakLocale": "",
    "cloakTimezone": "",
    "cloakGeoip": false,
    "cloakStealthArgs": true,
    "mode": "stealth",
    "browserPort": 9222,
    "autoConnect": false,
    "credentials": {}
  },
  "rescoreWeights": {
    "//": "Per-tool rescoring weights. Tune these to prioritize recency, citations, engagement, etc. rrfAnchor should dominate any single other signal.",
    "webSearch": {
      "rrfAnchor": 0.45,
      "domainAuthority": 0.25,
      "recency": 0.12,
      "yearAlignment": 0.12,
      "hasDeepLinks": 0.05
    },
    "academicSearch": {
      "rrfAnchor": 0.5,
      "recency": 0.05,
      "citations": 0.3,
      "venue": 0.15
    },
    "hackernewsSearch": {
      "rrfAnchor": 0.5,
      "recency": 0.15,
      "engagement": 0.2,
      "commentEngagement": 0.15
    },
    "redditSearch": {
      "rrfAnchor": 0.5,
      "recency": 0.1,
      "engagement": 0.25,
      "commentEngagement": 0.15
    }
  },
  "challengeLatencyThreshold": 5000
}
